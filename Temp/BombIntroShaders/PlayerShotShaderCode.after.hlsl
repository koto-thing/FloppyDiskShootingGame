struct VS_OUTPUT { float4 position : SV_POSITION; float2 uv : TEXCOORD0; }; cbuffer ShotBuffer : register(b0) { float4x4 u_transform; float4 u_color; float u_time; float u_shotType; float u_direction; float u_padding; }; VS_OUTPUT VSPlayerShot(uint vertexId : SV_VertexID) { VS_OUTPUT output; float2 localPosition; localPosition.x = float(vertexId & 2) - 1.0f; localPosition.y = float((vertexId & 1) << 1) - 1.0f; output.position = mul(float4(localPosition, 0.0f, 1.0f), u_transform); output.uv = localPosition; return output; } float4 PSPlayerShot(VS_OUTPUT input) : SV_TARGET { float2 uv = input.uv; float pulse = 0.88f + sin(u_time * 0.18f) * 0.12f; float3 color; float alpha; if (u_shotType < 0.5f) { float headDistance = length((uv - float2(0.38f, 0.0f)) * float2(1.0f, 1.35f)); float head = 1.0f - smoothstep(0.18f, 0.68f, headDistance); float tailWidth = 0.08f + saturate((uv.x + 1.0f) * 0.24f); float tail = (1.0f - smoothstep(tailWidth, tailWidth + 0.18f, abs(uv.y))) * saturate(1.0f - (uv.x - 0.10f) * 0.75f) * saturate(uv.x + 1.0f); float core = 1.0f - smoothstep(0.02f, 0.12f, abs(uv.y)); alpha = saturate(head + tail * 0.72f); color = lerp(float3(0.02f, 0.45f, 0.95f), float3(0.75f, 1.0f, 1.0f), saturate(head + core * tail)) * pulse; } else if (u_shotType < 1.5f) { float endMask = 1.0f - smoothstep(0.72f, 1.0f, abs(uv.x)); float core = 1.0f - smoothstep(0.02f, 0.13f, abs(uv.y)); float glow = 1.0f - smoothstep(0.08f, 0.72f, abs(uv.y)); alpha = saturate((core + glow * 0.65f) * endMask); color = lerp(float3(0.52f, 0.05f, 1.0f), float3(1.0f, 0.95f, 1.0f), core) * pulse; } else if (u_shotType < 2.5f) { float distanceFromCenter = length(uv); float core = 1.0f - smoothstep(0.05f, 0.34f, distanceFromCenter); float body = 1.0f - smoothstep(0.30f, 0.78f, distanceFromCenter); float ring = 1.0f - smoothstep(0.035f, 0.13f, abs(distanceFromCenter - 0.58f)); alpha = saturate(body + ring * 0.72f); color = lerp(float3(1.0f, 0.78f, 0.02f), float3(1.0f, 1.0f, 0.72f), saturate(core + ring * 0.35f)) * pulse; } else if (u_shotType < 3.5f) { float endMask = 1.0f - smoothstep(0.74f, 1.0f, abs(uv.x)); float core = 1.0f - smoothstep(0.02f, 0.19f, abs(uv.y)); float glow = 1.0f - smoothstep(0.10f, 0.74f, abs(uv.y)); alpha = saturate((core + glow * 0.48f) * endMask); color = lerp(float3(0.05f, 0.58f, 1.0f), float3(0.92f, 1.0f, 1.0f), core) * pulse; } else { float2 p = float2(uv.x, uv.y * 1.45f); float edgeNoise = sin(p.x * 18.0f + u_time * 0.21f) * 0.035f + sin(p.x * 31.0f - u_time * 0.13f) * 0.018f; float distanceFromCore = length(float2(p.x * 0.82f, p.y + edgeNoise)); float endMask = 1.0f - smoothstep(0.72f, 1.02f, abs(p.x)); float outerGlow = (1.0f - smoothstep(0.28f, 0.92f, distanceFromCore)) * endMask; float flame = (1.0f - smoothstep(0.14f, 0.58f, distanceFromCore)) * endMask; float core = (1.0f - smoothstep(0.035f, 0.20f, distanceFromCore)) * (1.0f - smoothstep(0.42f, 0.82f, abs(p.x))); alpha = saturate(outerGlow * 0.52f + flame * 0.72f + core); color = lerp(float3(0.72f, 0.002f, 0.001f), float3(1.0f, 0.075f, 0.008f), flame); color = lerp(color, float3(1.0f, 0.82f, 0.62f), core) * pulse; if (u_shotType > 5.5f) { color = lerp(float3(0.001f, 0.08f, 0.78f), float3(0.02f, 0.42f, 1.0f), flame); color = lerp(color, float3(0.68f, 0.94f, 1.0f), core) * pulse; } else if (u_shotType > 4.5f) color = 1.0f - saturate(color); } alpha *= saturate(u_color.a); if (alpha < 0.01f) discard; return float4(color, alpha); }