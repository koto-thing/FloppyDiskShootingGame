# ローカル2人協力プレイ

Floppy版を含む共通ゲーム本体で1人用／2人用を選択可能
通信やSteam SDKを使わず、Floppy版の配布上限1,474,560 bytesを維持する

## 始め方

1. コントローラーを2台接続してゲームを起動する
2. STARTから `2 PLAYERS` を選ぶ
3. 画面の案内に従って各コントローラーのショットボタンで参加を確定する
4. 難易度を選び、各プレイヤーが上下入力でショットタイプを選んで確定する
5. 2人とも確定すると開始する。同じタイプも選択可能

1Pは青、2Pは緑。1Pだけが視点切替を操作し、同じカメラでプレイする
片方のコントローラーが切断されたら自動ポーズし、両方を再接続して明示的に再開する
途中参加は行わない

## ゲームルール

- 残機は無限。被弾した自機だけ2秒後にその場へ復活し、約1.5秒の無敵が付く
- 被弾してもステージ・敵・相手の操作は進行する
- Powerは各自で保持し、被弾時に既存と同じ難易度別の量を失う（Easy 0.01、Normal 0.5、Hard 1.0、下限0）
- Powerアイテムを取得した本人だけが回復する。同時に接近した場合は最寄りの生存者へ引き寄せ、同距離では1Pを優先する
- ボムは各自の所持数から消費し、同時使用も可能。敵弾消去・敵への攻撃は共通
- スコアは2人で共有し、難易度別の協力用ローカルランキングへ記録する
- 既存の1人用ランキングは保持する。ランキング画面でSOLO／CO-OPを切替可能
- 敵数・耐久力・攻撃回数は1人用と同じ。協力専用の難易度補正は加えない

## 入力機器

XInput対応コントローラーと既存のNintendo Pro直接接続に対応する
同じ機器を直接入力と仮想XInput変換（Steam InputやBetterJoy等）で同時に公開しない
プレイ中はスロットを固定するため、接続する機器や接続方式を変える場合はゲームを再起動する

## 検証

`Tests/RunInputTests.ps1` で2台の入力分離・切断・再接続を検証する
`Tests/RunCoopTests.ps1` でゲーム状態・ステージ固有判定・既存ショットの回帰を検証する
実機では2台での参加、別々の操作、同時ボム、切断・再接続、2D/3D往復を試遊する
